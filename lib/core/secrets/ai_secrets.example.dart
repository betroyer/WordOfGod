/// Example template. Copy to `ai_secrets.dart` and paste your Groq key locally.
/// `ai_secrets.dart` is gitignored and must never be committed.
///
/// Get a key: https://console.groq.com/keys
class AiSecrets {
  static const apiKey = ''; // e.g. 'gsk_...'
}
